vazio principal {
    x = 2 * 2*(5+5);
    imprime(x);

    se (x > 20 e x < 41) {
        x = x - 1;
        imprime(x);
    }
    senao {
        imprime(1000);
    }
    
    top = 10;

    enquanto (top > 0) {
        top = top - 1;
        imprime(top);
    }
}