multiplica(x, y) {
    imprime(x*y);
}

principal(){
    x = escaneia();
    imprime(x);

    se (x > 20 e ((x < 41))) {
        x = x - 1;
        imprime(x);
    }

    senao {
        imprime(1000);
    }

    multiplica(3000, 3000);
    
    top = 10;

    enquanto (top > 0) {
        top = top - 1;
        imprime(top);
        gente = 5;
        imprime(gente);
    }
}